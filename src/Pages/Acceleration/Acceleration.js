import React from 'react';
import AccelerationCalculate from '../../Component/AccelerationCalculate/AccelerationCalculate';

const Acceleration = () => {
    return (
        <div>
            <AccelerationCalculate />
        </div>
    );
};

export default Acceleration;